# Copier en .env.local et remplir (Dashboard Supabase → Settings → API).
# Sans ces valeurs, l'app fonctionne en mode local uniquement (sans compte).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Web Push (VAPID) — clé publique uniquement
VITE_VAPID_PUBLIC_KEY=
